services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - DB_PATH=/app/data/berletek.db
    volumes:
      - db:/app/data
    networks:
      - berletek

volumes:
  db:

networks:
  berletek:
    driver: bridge
